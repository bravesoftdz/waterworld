program WENapp;

uses
  Vcl.Forms,
  MainUnit in 'MainUnit.pas' {frmMainForm},
  RawDataView in 'RawDataView.pas' {RawDataForm},
  Vcl.Themes,
  Vcl.Styles,
  EditDatabase in 'EditDatabase.pas' {frmDatabaseEdit};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TfrmMainForm, MainForm);
  Application.Run;
end.
