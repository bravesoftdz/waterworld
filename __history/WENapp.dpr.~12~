program WENapp;

uses
  Vcl.Forms,
  MainUnit in 'MainUnit.pas' {Form1},
  RawDataView in 'RawDataView.pas' {Form2},
  Vcl.Themes,
  Vcl.Styles,
  EditDatabase in 'EditDatabase.pas' {frmDatabaseEdit};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, MainForm);
  Application.Run;
end.
